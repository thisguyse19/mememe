/** Four-letter codes → destination label shown in the loading state (URL wiring comes later). */
export const SECRET_DESTINATIONS: Record<string, string> = {
  test: 'The Workshop',
}

export const CODE_LENGTH = 4
